<div class="col-8 m-auto shadow-sm p-3 mb-5 bg-body rounded">
  <h1>MY TASKS</h1>
  <% @tasks.each do |task| %>
    <div class="form-check">

      <%= task.completed ? '✔️' : '❌' %>
      <label for="<%= task.title %>" class="form-check-label"><%= link_to task.title, task_path(task.id), class: "text-decoration-none text-black" %></label>
      .
      <%= link_to "🗑️", destroy_task_path(task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "text-decoration-none" %>
    </div>
  <% end %>
  <br>
  <div class="border-top">
    <p><%= link_to "➕ Add a new task", new_task_path, class: "text-decoration-none" %></p>
  </div>

</div>
